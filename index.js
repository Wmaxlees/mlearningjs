'use strict';

{
    var Network = require(__dirname + '/Network')
        , Tensor3D = require(__dirname + '/tensors/Tensor3D');

    module.exports = {
        Network: Network
        , Tensor3D: Tensor3D
    };

}